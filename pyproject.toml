[project]
name = "onpe-results"
version = "0.1.0"
description = "Regional-level scraper for ONPE election result routes"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32.5",
]
